export {
  configureScaler,
  deviceInfo,
  getPixelSize,
  getScales,
  onOrientationChange,
  responsive,
  responsiveStyles,
  scale,
  scaleFont,
  scaleHeight,
  scaleLineHeight,
  scaleRadius,
  scaleWidth,
  scales,
  screenHeight,
  screenWidth,
} from './responsive';

export type {
  DeviceInfo,
  FontScaleOptions,
  ScaleOptions,
} from './responsive';

